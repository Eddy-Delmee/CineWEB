{% extends 'base.html.twig' %}

{% block title %}Genres
{% endblock %}

{% block body %}
	<h1 class="text-center backgroundShow">Genres</h1>

	<table class="table table-dark table-striped mb-2">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ category.id }}</td>
			</tr>
			<tr>
				<th>NameCategory</th>
				<td>{{ category.nameCategory }}</td>
			</tr>
		</tbody>
	</table>

	<div class="text-center backgroundShow mt-2">
		<a href="{{ path('app_categories_index') }}" class="btn btn-light mb-3">Retour</a>

		<a href="{{ path('app_categories_edit', {'id': category.id}) }}" class="btn btn-success mb-3">Editer</a>

		{{ include('categories/_delete_form.html.twig') }}
	</div>
{% endblock %}
