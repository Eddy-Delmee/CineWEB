{% extends 'base.html.twig' %}

{% block title %}Recommendations
{% endblock %}

{% block body %}
	<h1 class="text-center grey yellow">Recommendations</h1>

	<table class="table table-dark table-striped mb-2">
		<tbody>
			<tr>
				<th>ID</th>
				<td>{{ recommendation.id }}</td>
			</tr>
			<tr>
				<th>Restrictions Âges</th>
				<td>{{ recommendation.forbiddenAge }}</td>
			</tr>
		</tbody>
	</table>

	<div class="text-center backgroundShow mt-2">
		<a href="{{ path('app_recommendations_index') }}" class="btn btn-light mb-3">Retour</a>

		<a href="{{ path('app_recommendations_edit', {'id': recommendation.id}) }}" class="btn btn-success mb-3">Editer</a>

		{{ include('recommendations/_delete_form.html.twig') }}
	</div>
{% endblock %}
