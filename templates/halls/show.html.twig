{% extends 'base.html.twig' %}

{% block title %}Halls{% endblock %}

{% block body %}
    <h1>Halls</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hall.id }}</td>
            </tr>
            <tr>
                <th>NameHall</th>
                <td>{{ hall.nameHall }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_halls_index') }}">back to list</a>

    <a href="{{ path('app_halls_edit', {'id': hall.id}) }}">edit</a>

    {{ include('halls/_delete_form.html.twig') }}
{% endblock %}
