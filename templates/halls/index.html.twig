{% extends 'base.html.twig' %}

{% block title %}Halls index{% endblock %}

{% block body %}
    <h1>Halls index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NameHall</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for hall in halls %}
            <tr>
                <td>{{ hall.id }}</td>
                <td>{{ hall.nameHall }}</td>
                <td>
                    <a href="{{ path('app_halls_show', {'id': hall.id}) }}">show</a>
                    <a href="{{ path('app_halls_edit', {'id': hall.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_halls_new') }}">Create new</a>
{% endblock %}
