{% extends 'base.html.twig' %}

{% block title %}News{% endblock %}

{% block body %}
    <h1>News</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ news.id }}</td>
            </tr>
            <tr>
                <th>TitleNew</th>
                <td>{{ news.titleNew }}</td>
            </tr>
            <tr>
                <th>ImageNew</th>
                <td>{{ news.imageNew }}</td>
            </tr>
            <tr>
                <th>VideoNew</th>
                <td>{{ news.videoNew }}</td>
            </tr>
            <tr>
                <th>DescriptionNew</th>
                <td>{{ news.descriptionNew }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_news_index') }}">back to list</a>

    <a href="{{ path('app_news_edit', {'id': news.id}) }}">edit</a>

    {{ include('news/_delete_form.html.twig') }}
{% endblock %}
